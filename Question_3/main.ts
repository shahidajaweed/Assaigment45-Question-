let Name: string = "Shahida";   // this save the name
console.log(Name.toLowerCase());   // show the name in all small letters

console.log(Name.toUpperCase());  // show the name in all big letters

//show the name with the first letter big
console.log(Name.charAt(0).toUpperCase() + Name.slice(1).toLowerCase());
